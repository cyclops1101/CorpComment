import { Header, FeedbackList } from "..";

const Container = () => {
  return (
    <main className="container">
      <Header />
      <FeedbackList />
    </main>
  );
};

export default Container;
